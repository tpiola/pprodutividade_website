# =============================================================================
# Environment Variables Configuration Example
# =============================================================================
# Copy this file to .env and fill in the actual values
# DO NOT commit .env files with real secrets to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MySQL/MariaDB connection string
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://user:password@localhost:3306/pprodutividade

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
# Secret key for JWT token signing (use a strong random string in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# OpenID of the repository/app owner (required for admin access)
OWNER_OPEN_ID=owner-openid-here

# OAuth server URL for authentication
OAUTH_SERVER_URL=https://your-oauth-server.com

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public API key
SUPABASE_ANON_KEY=your-supabase-anon-key

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application ID for the frontend
VITE_APP_ID=your-app-id

# OAuth portal URL for frontend authentication
VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com

# Built-in Forge API configuration for backend
BUILT_IN_FORGE_API_URL=https://api.forge.example.com
BUILT_IN_FORGE_API_KEY=your-forge-api-key

# Frontend Forge API configuration (optional, for Map component)
VITE_FRONTEND_FORGE_API_URL=https://api.forge.example.com
VITE_FRONTEND_FORGE_API_KEY=your-frontend-forge-api-key

# -----------------------------------------------------------------------------
# Analytics Configuration (Optional)
# -----------------------------------------------------------------------------
# Analytics endpoint URL (if using analytics)
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com

# Analytics website ID (if using analytics)
VITE_ANALYTICS_WEBSITE_ID=your-website-id

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Node environment (development, production, test)
NODE_ENV=development

# Port for the Express server (defaults to 3000 if not set)
PORT=3000

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. All variables prefixed with VITE_ are exposed to the frontend build
# 2. Never commit real secrets or API keys to version control
# 3. In production, set these in your deployment environment or CI/CD secrets
# 4. Required variables: DATABASE_URL, JWT_SECRET, VITE_APP_ID, OWNER_OPEN_ID
# 5. For CI/CD: Configure GitHub Secrets with production values
# =============================================================================
