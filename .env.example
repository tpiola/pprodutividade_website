# Database Configuration
# Required: MySQL connection string for the application database
# Example: mysql://user:password@localhost:3306/database_name
DATABASE_URL=mysql://user:password@localhost:3306/pprodutividade_website

# Authentication & Security
# Required: Secret key for JWT token generation and cookie signing
# Generate a strong random string (e.g., using: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here

# OAuth Configuration
# Required: Application ID registered in the OAuth portal
VITE_APP_ID=your-app-id

# Required: OAuth portal URL for authentication
VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com

# Required: OAuth server URL for token validation
OAUTH_SERVER_URL=https://your-oauth-server.com

# Required: OpenID of the application owner
OWNER_OPEN_ID=owner-open-id

# Supabase Configuration (Optional)
# Optional: Supabase project URL - only needed if using Supabase features
# SUPABASE_URL=https://your-project.supabase.co

# Optional: Supabase anonymous key - only needed if using Supabase features
# SUPABASE_ANON_KEY=your-supabase-anon-key

# Analytics Configuration (Optional)
# Optional: Analytics endpoint URL (e.g., Umami analytics)
# VITE_ANALYTICS_ENDPOINT=https://analytics.example.com

# Optional: Website ID for analytics tracking
# VITE_ANALYTICS_WEBSITE_ID=your-website-id

# Forge API Configuration (Optional)
# Optional: Built-in Forge API URL for backend services
# BUILT_IN_FORGE_API_URL=https://api.forge.example.com

# Optional: Built-in Forge API key for backend authentication
# BUILT_IN_FORGE_API_KEY=your-forge-api-key

# Optional: Frontend Forge API URL for client-side map services
# VITE_FRONTEND_FORGE_API_URL=https://api.forge.example.com

# Optional: Frontend Forge API key for client-side authentication
# VITE_FRONTEND_FORGE_API_KEY=your-frontend-forge-api-key

# Server Configuration
# Optional: Port for the application server (defaults to 3000)
# PORT=3000

# Optional: Node environment (set automatically by scripts: development, production)
# NODE_ENV=development
