# ==============================================================================
# ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# ==============================================================================
# 
# IMPORTANTE: 
# - Copie este arquivo para .env e preencha com valores reais
# - NUNCA comite o arquivo .env com chaves reais no repositório
# - O arquivo .env está listado no .gitignore para sua segurança
#
# ==============================================================================

# ------------------------------------------------------------------------------
# NODE.JS ENVIRONMENT
# ------------------------------------------------------------------------------
NODE_ENV=development                    # Ambiente: development | production | test
PORT=3000                              # Porta do servidor (padrão: 3000)

# ------------------------------------------------------------------------------
# FORGE API (BACKEND - server/_core)
# ------------------------------------------------------------------------------
# Chaves usadas pelo servidor Node.js para serviços de backend (imagens, storage, etc)
# Como obter: Solicite acesso ao painel Forge/Manus ou configure seu próprio proxy
BUILT_IN_FORGE_API_URL=https://forge.example.com
BUILT_IN_FORGE_API_KEY=your_forge_api_key_here

# ------------------------------------------------------------------------------
# FORGE API (FRONTEND - client)
# ------------------------------------------------------------------------------
# Chaves usadas pelo frontend React para serviços como Google Maps proxy
# Como obter: Use a mesma chave do backend ou crie uma chave específica para frontend
VITE_FRONTEND_FORGE_API_URL=https://forge.example.com
VITE_FRONTEND_FORGE_API_KEY=your_frontend_forge_api_key_here

# ------------------------------------------------------------------------------
# GOOGLE MAPS (FRONTEND)
# ------------------------------------------------------------------------------
# Chave direta do Google Maps (se não usar proxy Forge)
# Como obter: https://console.cloud.google.com/apis/credentials
# Habilitar: Maps JavaScript API, Places API, Geocoding API
VITE_FRONTEND_GOOGLE_MAPS_KEY=your_google_maps_api_key_here

# ------------------------------------------------------------------------------
# AWS S3 STORAGE
# ------------------------------------------------------------------------------
# Credenciais para armazenamento de arquivos no S3
# Como obter: AWS Console > IAM > Users > Security Credentials
# Permissões necessárias: s3:PutObject, s3:GetObject, s3:DeleteObject
AWS_S3_BUCKET=your-bucket-name
AWS_S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# ------------------------------------------------------------------------------
# ANALYTICS
# ------------------------------------------------------------------------------
# Configuração para rastreamento de analytics (ex: Plausible, Umami, etc)
# Como obter: Painel do seu serviço de analytics
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com
VITE_ANALYTICS_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ------------------------------------------------------------------------------
# DATABASE
# ------------------------------------------------------------------------------
# URL de conexão com o banco de dados (MySQL/PostgreSQL/SQLite)
# Formato MySQL: mysql://user:password@host:port/database
# Formato PostgreSQL: postgresql://user:password@host:port/database
DATABASE_URL=mysql://user:password@localhost:3306/pprodutividade

# ------------------------------------------------------------------------------
# OAUTH & AUTHENTICATION
# ------------------------------------------------------------------------------
# Configurações para autenticação OAuth
# Como obter: Configure no seu servidor OAuth ou use serviço como Auth0, Supabase, etc
VITE_APP_ID=your_app_id_here
VITE_OAUTH_PORTAL_URL=https://oauth.example.com
OAUTH_SERVER_URL=https://oauth.example.com
JWT_SECRET=your_jwt_secret_minimum_32_characters_long_random_string
OWNER_OPEN_ID=owner_identifier_here

# ------------------------------------------------------------------------------
# NOTAS IMPORTANTES PARA EXPORT ESTÁTICO
# ------------------------------------------------------------------------------
# 
# Ao gerar o export estático (pnpm run export:static), os recursos dinâmicos
# serão substituídos por placeholders:
# 
# - Mapas do Google: Serão renderizados com dados mock ou placeholder
# - Imagens geradas (IA): Retornarão placeholders estáticos
# - APIs externas: Usarão dados mock quando as chaves não estiverem disponíveis
#
# Para produção, você precisará:
# 1. Configurar as chaves reais no servidor de hospedagem
# 2. Substituir placeholders por conteúdo real no Elementor
# 3. Configurar CORS e domínios permitidos
#
# ==============================================================================
